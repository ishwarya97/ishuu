#include<stdio.h>
int main()
char str1[25],str2[50];
int i=0,j=0;
printf("the first string:");
gets(str1);
printf("the second string:");
gets(str2);
if(str1>str2)
{
printf("concatinate the two strings');
}

}

